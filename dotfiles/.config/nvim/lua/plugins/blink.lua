return {
  "saghen/blink.cmp",
  opts = {
    fuzzy = {
      prebuilt_binaries = {
        download = true,
        -- uncomment if your platform/arch needs a specific tag
        -- force_version = "1.0.0",
      },
    },
  },
}
