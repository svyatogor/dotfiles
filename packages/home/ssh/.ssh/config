# SSH home config (template)
#
# This file is stowed only when CONTEXT=home.
# Put sensitive host entries in ~/.ssh/config.home.local (not checked-in).

Include ~/.ssh/config.home.local

# Example:
# Host nas
#   HostName nas.local
#   User sergey
#   IdentityFile ~/.ssh/id_ed25519

