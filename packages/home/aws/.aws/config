# AWS CLI config (template)
#
# This file is stowed only when CONTEXT=home.
# Do not put secrets here. For credentials, use ~/.aws/credentials (ignored) or SSO.

#[default]
#region = eu-central-1
#output = json

