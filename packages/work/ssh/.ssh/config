# SSH work config (template)
#
# This file is stowed only when CONTEXT=work.
# Put sensitive host entries in ~/.ssh/config.work.local (not checked-in).

Include ~/.ssh/config.work.local

# Example:
# Host work-bastion
#   HostName bastion.example.com
#   User ubuntu
#   IdentityFile ~/.ssh/id_ed25519

